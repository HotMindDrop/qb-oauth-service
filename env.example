# QuickBooks OAuth Configuration
CLIENT_ID=your_intuit_client_id
CLIENT_SECRET=your_intuit_client_secret
REDIRECT_URI=https://YOUR-RENDER-URL.onrender.com/oauth/callback

# Environment (sandbox or production)
QBO_ENV=sandbox

# Security
STATE_SECRET=some-long-random-string-for-security

# API Version
MINOR_VERSION=70

# Server Configuration (optional - defaults to 10000)
PORT=10000